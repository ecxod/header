<!DOCTYPE html>
<html lang="{{ lang|raw }}">
	<head>
        {{ lang|raw }}
        {{ encTYPE|raw }}
        {{ viewport|raw }}
        {{ cash|raw }}
        {# {{ description|raw }} #}
        {# {{ icons|raw }} #}
        {# {{ apple|raw }} #}
        {# {{ twitter|raw }} #}
        {{ loadBootstrapCssAndIcons|raw }}
        {{ loadStyles|raw }}
        {# {{ autor|raw }} #}
        {# {{ datum|raw }} #}
        {# {{ canonical|raw }} #}
        {{ loadScripts|raw }}
    </head>
</html>
